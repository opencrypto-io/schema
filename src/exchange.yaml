$id: "https://schema.coinspec.io/exchange#"
type: object
properties:
  name:
    title: Name
    type: string
  web:
    oneOf:
      - type: string
        format: url
      - type: array
        items:
          type: string
          format: url
  markets:
    type: array
    items:
      type: string
additionalProperties: false
