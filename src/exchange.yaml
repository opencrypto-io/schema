$id: "https://schema.coinspec.io/exchange#"
type: object
properties:
  id:
    type: string
  name:
    title: Name
    type: string
  countries:
    type: array
    items:
      type: string
      pattern: '^(\w){2}$'
  web:
    $ref: '#/definitions/links'
  api:
    url:
      $ref: '#/definitions/links'
  markets:
    type: array
    items:
      type: string
additionalProperties: false
definitions:
  links:
    oneof:
      - type: string
        format: url
      - type: array
        items:
          type: string
          format: url
